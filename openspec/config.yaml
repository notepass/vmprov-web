schema: spec-driven

context: |
  Tech stack: Go 1.21+, Echo web framework, slog (stdlib logging), viper for config, testify for tests
  Config: YAML file + env vars (SERVER_PORT, DB_CONN_STRING, DB_USERNAME, DB_PASSWORD, LOG_LEVEL)
  Project layout: standard Go (cmd/server/, internal/)
  Module path: github.com/notepass/vmprov-web
  Build: Go modules + Makefile (build, run, test targets)
  Domain: VM cloud-init provisioning via libvirt

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
